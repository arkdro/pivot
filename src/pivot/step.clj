(ns pivot.step
  (:require pivot.misc)
  )

;; {:n n-items :c capacity :items items}
(defn calc [{n-items :n
             capacity :c
             items :items
             :as data}]
  (let [
        ]
    {:opt
     :val
     })
  )

